import math
def cylinder_volume(radius, height):
  """ float, float -> None
  function cylinder_volume takes in parameters radius
  and height and prints the volume of a cylinder
  """
  print(math.pi * height * (radius ** 2))
